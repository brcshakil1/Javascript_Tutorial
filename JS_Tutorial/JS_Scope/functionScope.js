/* function scope call inside of function. the function scope's 
variables are not accessed outside of function. */

function myFunction(){

    const x = 6;
    let y = 7;
}

// x and y cannot use here 