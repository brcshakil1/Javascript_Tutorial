// block scope's variable use inside of {} curly brackets 
// var variable cannot be block scope. because var can be access outside of {} curly brackets.

{
    const x = 5;
    let y = 8;
}

// x and y cannot use here 